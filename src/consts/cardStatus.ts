import { ICardStatus } from '../types/cardStatus';

export const cardStatus: ICardStatus = {
	CLASSIC: 'classic',
	STOPPED: 'stopped',
	ONLINE: 'online',
	SERVER_SIDE: 'server-side',
	DRAFT: 'draft',
	PAUSED: 'paused',
};
